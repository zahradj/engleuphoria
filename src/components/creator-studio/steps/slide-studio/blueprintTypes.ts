// Re-export from the shared module — kept for backwards compatibility while we
// retire the standalone Slide Studio. New code should import from
// `@/components/creator-studio/shared/blueprintTypes`.
export * from '../../shared/blueprintTypes';
